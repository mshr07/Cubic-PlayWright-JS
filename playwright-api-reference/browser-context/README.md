# Browser Context

This category covers methods related to browser lifecycle and browser contexts.

Why it matters:

- contexts isolate sessions
- contexts control cookies and auth state
- contexts are core to parallel and multi-user testing

Recommended order:

1. `browser-and-context-methods.js`
2. `context-timeouts-and-permissions.js`

